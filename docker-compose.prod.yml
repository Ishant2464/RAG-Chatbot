services:
  app:
    build: .
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    env_file:
      - .env.prod
    depends_on:
      - worker

  worker:
    build: .
    env_file:
      - .env.prod
    command: python -m app.worker
