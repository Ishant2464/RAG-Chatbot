services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env.prod
    command: uvicorn app.api.main:app --host 0.0.0.0 --port 8000
    depends_on:
      - worker

  worker:
    build: .
    env_file:
      - .env.prod
    command: python -m app.worker
